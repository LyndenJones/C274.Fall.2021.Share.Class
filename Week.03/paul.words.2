Modify the solution to Part 2 above, or modify Example Problem (4)
(aka count.the.p4.v1.py), so that it has a proper “main” section,
that it can be import-ed by a different Python program, and so that
function safe_input() can be called by a different “main”.
the
course
honours
cs
and
ce
